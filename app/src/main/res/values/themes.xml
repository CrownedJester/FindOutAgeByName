<resources>
    <!-- Base application theme. -->
    <style name="Theme.FindOutAgeByName" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/tea_green</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/gunmetal</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/gunmetal</item>
    </style>

    <style name="CheckboxFavorite" parent="Widget.Material3.CompoundButton.CheckBox">
        <item name="strokeColor">@color/rhythm</item>
        <item name="buttonTint">@color/rhythm</item>
    </style>

    <style name="ProgressTheme">
        <item name="colorAccent">@color/gunmetal</item>
    </style>

</resources>